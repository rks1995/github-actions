name: test workflow

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: test action
        run: echo "Hello World"
        shell: bash

  test-terminate:
    runs-on: ubuntu-latest
    steps:
      - name: terminate test
        run: echo "Good bye World"
        shell: bash
